"""Orbit standalone runtime kernel.

Local brain, local agent runtime, and local scheduler. Requires no vendor API,
no network, and no credentials. External providers are optional accelerators
that plug in behind the same interfaces.
"""
